class Solution {
public:
    int minPartitions(string str){
        int n=str.size();
        int ans=0;
        for(int i=0;i<n;i++){
            char ch=str[i];
            int num=ch-'0';
            ans=max(ans,num);

        }
        return ans;



        
    }
};
